import DashboardLayout from '@/components/DashboardLayout';

export default function SplitTemplates() {
  return (
    <DashboardLayout>
      <div className="p-8">
        <h1 className="text-3xl font-bold mb-2">Split Templates</h1>
        <p className="text-muted-foreground mb-8">Create reusable percentage split presets for fast release setup</p>
        <div className="rounded-xl border border-muted/20 bg-muted/10 p-8 text-center text-lg text-muted-foreground">Coming soon: Save and reuse your split presets.</div>
      </div>
    </DashboardLayout>
  );
} 